{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only plugin permissions (notification, autostart, global-shortcut). These plugins are compiled and registered only under cfg(desktop), so their permission ids must not resolve on Android/iOS capability resolution — scoped to desktop platforms to match.",
  "platforms": [
    "macOS",
    "windows",
    "linux"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "notification:default",
    "autostart:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered"
  ]
}
